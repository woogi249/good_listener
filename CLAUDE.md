# Realtime Panel

한국어 음성 회의를 실시간으로 분석해 4개 패널(요약/팩트체크/아이디어/반박)을 팀 공용 화면에 표시하고, 회의 종료 후 실행 항목과 결정 로그를 일괄 정리하는 회의 코파일럿.

---

## 프로젝트 목적

- 한국어 음성 회의 중 맥락 유실, 잘못된 전제, 교착, 집단사고를 실시간으로 교정
- 회의 종료 시 실행 항목(Action Item)과 결정 사항(Decision Log)을 자동 정리
- 팀 전원이 한 화면을 공유하며 AI 보조를 받도록 구성

---

## 사용 맥락

- **입력:** 회의실 마이크 → STT → 롤링 트랜스크립트 버퍼
- **출력:** 팀 공용 디스플레이에 4개 패널 실시간 갱신 + 회의 종료 후 요약 리포트
- **언어:** 한국어 (STT/분석/출력 모두 한국어 전제)
- **사용자:** 팀 공용 (개인용 아님)

---

## 핵심 제약

### 비용 최소화 (최상위 원칙)
- **Claude API 직접 호출 금지.** 사용자는 Claude Pro/Max 구독을 통해 `claude` CLI를 사용 중이며, 이 프로젝트는 **CLI 서브프로세스 호출로만 분석을 수행**해야 함. API 키 기반 직접 호출은 금지.
- CLI headless mode (`claude -p "프롬프트"` 또는 stdin pipe)를 활용해 non-interactive 분석.
- STT도 유료 클라우드(Google/Azure/Clova) 금지. 로컬 Whisper 우선.
- 유료 서비스가 꼭 필요한 경우 사용자에게 먼저 확인 후 진행.

### 실시간성 vs 비용 트레이드오프
- 매 발화마다 4개 패널 모두 돌리지 않음. 패널별 **트리거 조건**으로 호출 빈도 제어.
- CLI 호출은 비동기 큐로 처리, UI는 결과 도착 시 부분 업데이트.
- 롤링 버퍼 크기 제한(직전 10~20 발화)으로 프롬프트 토큰 최소화.

### 패널 구성 (확정)

**실시간 4개 (회의 중 디스플레이):**
| 패널 | 이름 | 트리거 | 출력 제약 |
|------|------|--------|----------|
| A | Summarizer (요약) | 매 N발화 또는 주제 전환 감지 | 1줄, 최대 50자, 명사형 종결 |
| B | Fact-Checker (팩트체크) | 수치/고유명사/기술적 주장 감지 시 | 1줄, 최대 50자, 오류 없으면 출력 없음 |
| C | Ideator (아이디어) | 교착/반복 감지 시 | 1줄, 최대 50자, 질문형 종결 |
| D-3 | Devil's Advocate (반박) | 합의 신호 감지 시 | 1줄, 최대 50자, 리스크 지적 |

**사후 2개 (회의 종료 후 일괄):**
| 패널 | 이름 | 출력 |
|------|------|------|
| D-1 | Action Item Tracker | 누가/무엇을/언제까지 형태 목록 |
| D-2 | Decision Log | 확정된 결정 사항 목록 (번복된 항목 제외) |

---

## 기술 스택 방향

| 영역 | 후보 기술 | 사유 |
|------|----------|------|
| STT | faster-whisper (로컬, Python) 또는 whisper.cpp | 무료, 한국어 지원, 오프라인 |
| 분석 엔진 | `claude` CLI subprocess | 구독 내 비용, API 키 불필요 |
| 백엔드 | Python (FastAPI) 또는 Node.js | STT 파이프라인 + CLI 서브프로세스 관리 |
| UI | 웹 (브라우저) — React 또는 경량 HTMX | 팀 공용 디스플레이, 기기 독립적 |
| 실시간 통신 | WebSocket 또는 SSE | 패널별 독립적 스트리밍 |
| 배포 | 로컬 실행 (Electron 래핑 고려) | 회의실 PC에서 `앱 실행` 시 백엔드도 함께 기동 |

> **최종 스택은 deep-interview 단계에서 확정.** 특히 STT 품질(한국어 정확도)과 CLI 호출 레이턴시 측정 후 결정.

---

## 실행 방식 (컨셉)

```
사용자: 앱 실행
  ↓
앱이 백엔드 기동 (STT + CLI 디스패처)
  ↓
마이크 캡처 → STT → 롤링 버퍼
  ↓
트리거 조건 충족 시 → claude CLI subprocess 호출
  ↓
CLI stdout 스트림 → WebSocket → 브라우저 패널 갱신
  ↓
회의 종료 버튼 → D-1, D-2 일괄 실행 → 리포트 생성
```

---

## 핵심 불확실성 (deep-interview에서 해소 필요)

1. **Claude CLI의 동시 호출 한계:** 4개 패널이 병렬로 CLI를 띄울 수 있는지, 또는 단일 세션에서 순차 처리해야 하는지.
2. **CLI 호출 레이턴시:** 한 번 호출에 몇 초 걸리는지 측정. 실시간성 확보 가능한지 판단.
3. **STT 레이턴시:** 로컬 Whisper의 한국어 정확도와 지연. 스트리밍 vs 배치 처리.
4. **트리거 조건 구현:** "교착 감지", "합의 신호 감지" 같은 추상 조건을 어떻게 룰 또는 가벼운 모델로 구현할지.
5. **화면 레이아웃:** 4개 패널 동시 표시 시 인지부하. 우선순위/강조 UI 필요 여부.
6. **개인정보/보안:** 회의 음성이 로컬에만 머무는지, CLI 프롬프트로 전송되는 내용 범위.

---

## 개발 규칙

- **API 직접 호출 금지.** 모든 AI 분석은 `claude` CLI 서브프로세스를 경유.
- 프롬프트는 별도 파일(`prompts/`)로 분리. 패널별 지시사항은 버전 관리.
- 롤링 버퍼는 메모리 상주, 세션 종료 시 옵션으로만 저장(기본은 폐기).
- UI 패널 출력은 **절대 임의 확장하지 않음.** 1줄/50자 제약 엄수.
- 트리거 조건은 설정 파일로 외부화하여 튜닝 가능하게.
